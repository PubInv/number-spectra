html {
  width: 100%;
  display: table;
}

html, body {
  height: 100%;
}

body {
  background: none;
  background-image: linear-gradient(to bottom, #fcdea0, #ff7a85);
  background-repeat: no-repeat;
  color: #444;
  display: grid;
}

a {
  color: #2b2b2b;
}
a:hover, a:focus {
  color: #515151;
}
a:active {
  color: #515151;
}

input {
  width: 100%;
}

input[type=range] {
  opacity: .5;
}

input[type=number],
input[type=text] {
  border-color: transparent;
  color: #444;
}
input[type=number], input[type=number]:focus,
input[type=text],
input[type=text]:focus {
  background-color: rgba(255, 122, 133, 0.2);
}

.grid-item--bottom {
  align-self: end;
}

.text--center {
  text-align: center;
}

.stage {
  display: grid;
  grid-gap: 1.5em;
  grid-auto-flow: column;
}

.stage__controls {
  display: grid;
  justify-self: center;
  padding-left: 1.5em;
  padding-top: 1.5em;
  max-width: 640px;
}

.stage__mount {
  display: grid;
}

.mount {
  margin: 0 auto;
  max-width: 200px;
  width: 100%;
}
.mount svg {
  display: block;
}

svg {
  max-width: 100%;
  height: auto;
}

.row {
  max-width: 560px;
}
